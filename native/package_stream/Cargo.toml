[package]
name = "package_stream"
version = "0.1.0"
edition = "2021"

[lib]
name = "package_stream"
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
rustler = "0.37"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
brotli = "8.0.2"
